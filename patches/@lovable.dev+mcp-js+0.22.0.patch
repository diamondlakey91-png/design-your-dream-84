diff --git a/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.cjs b/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.cjs
index 0bbc568..fe846bf 100644
--- a/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.cjs
+++ b/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.cjs
@@ -44,7 +44,9 @@ function normalizePath(p) {
   return p.split(import_node_path.sep).join("/");
 }
 function assertContains(parent, child, label) {
-  if (child !== parent && !child.startsWith(parent + import_node_path.sep)) {
+  const normParent = normalizePath(parent);
+  const normChild = normalizePath(child);
+  if (normChild !== normParent && !normChild.startsWith(normParent + "/")) {
     throw new Error(`@lovable.dev/mcp-js: ${label} must resolve under ${parent}, got ${child}`);
   }
 }
diff --git a/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.js b/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.js
index e796e48..01b3e08 100644
--- a/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.js
+++ b/node_modules/@lovable.dev/mcp-js/dist/stacks/tanstack/vite.js
@@ -13,7 +13,9 @@ function normalizePath(p) {
   return p.split(sep).join("/");
 }
 function assertContains(parent, child, label) {
-  if (child !== parent && !child.startsWith(parent + sep)) {
+  const normParent = normalizePath(parent);
+  const normChild = normalizePath(child);
+  if (normChild !== normParent && !normChild.startsWith(normParent + "/")) {
     throw new Error(`@lovable.dev/mcp-js: ${label} must resolve under ${parent}, got ${child}`);
   }
 }
